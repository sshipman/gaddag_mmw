name: gaddag_mmw
description: A starting point for Dart libraries or applications.
version: 1.0.0
# repository: https://github.com/sshipman/GADDAG_mmw

environment:
  sdk: ^3.10.0

# Add regular dependencies here.
dependencies:
  meta: ^1.18.2
  # path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
